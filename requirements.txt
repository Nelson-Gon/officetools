# if unsure run this to install required programs for these scripts

#LibreOffice 6.0.7.3 00m0(Build:3)

sudo apt update -y && sudo apt upgrade -y
sudo apt install -y libreoffice

# pandoc 1.19.2.4

sudo apt install -y pandoc
